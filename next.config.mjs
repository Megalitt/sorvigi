/** @type {import('next').NextConfig} */
const nextConfig = {
	// basePath: "/sorvigi",
	output: "export",
	// reactStrictMode: true, 
	// output: "standalone", 
	// trailingSlash: true,
};

export default nextConfig;
